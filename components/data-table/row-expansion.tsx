"use client"

import React from "react"

export function RowExpansion({ children }: { children?: React.ReactNode }) {
  // Lightweight placeholder. Replace with Sheet/Drawer for full UX.
  return <div className="p-4">{children}</div>
}

export default RowExpansion
